import { useCallback, useEffect, useMemo, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { BadgeCheck, CircleDollarSign, Clock3, Copy, LogOut, ReceiptText, RefreshCw, TicketPercent, WalletCards } from 'lucide-react';
import { supabase } from '../lib/supabaseClient';
import { Toaster, toast } from 'sonner';

const money = value => Number(value || 0).toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' });
const date = value => value ? new Date(value).toLocaleDateString('pt-BR', { dateStyle: 'medium' }) : '—';

const PartnerPortal = () => {
  const navigate = useNavigate();
  const [data, setData] = useState(null); const [loading, setLoading] = useState(true);
  const load = useCallback(async () => { setLoading(true); const { data: response, error } = await supabase.functions.invoke('partner-portal', { body: { action: 'dashboard' } }); if (error || response?.error) { if (response?.error === 'unauthorized' || response?.error === 'partner_not_found') { await supabase.auth.signOut(); navigate('/parceiros/entrar'); } else toast.error('Não foi possível atualizar seu painel.'); } else setData(response); setLoading(false); }, [navigate]);
  useEffect(() => { const timer = window.setTimeout(load, 0); return () => window.clearTimeout(timer); }, [load]);
  const metrics = useMemo(() => data?.metrics || { paidSalesCount: 0, paidSalesAmount: 0, pendingCommission: 0, availableCommission: 0, paidCommission: 0 }, [data]);
  const copyCoupon = async () => { if (!data?.coupon?.code) return; await navigator.clipboard.writeText(data.coupon.code); toast.success('Cupom copiado.'); };
  const signOut = async () => { await supabase.auth.signOut(); navigate('/parceiros/entrar'); };
  if (loading) return <div className="partner-portal-loading">Carregando seu painel...</div>;
  const profile = data?.profile;
  if (!profile) return null;
  if (profile.status !== 'approved') return <div className="partner-portal-shell"><Toaster richColors /><header className="partner-portal-header"><strong>GRAPHÈNE <small>PARCEIROS</small></strong><button onClick={signOut}><LogOut size={16}/> Sair</button></header><main className="partner-status-card"><Clock3/><span>SOLICITAÇÃO {profile.status === 'rejected' ? 'NÃO APROVADA' : 'EM ANÁLISE'}</span><h1>{profile.status === 'rejected' ? 'Sua solicitação precisa de revisão.' : 'Estamos analisando sua parceria.'}</h1><p>{profile.status === 'rejected' ? profile.rejection_reason : 'Assim que o cadastro for aprovado, seu cupom e todas as informações de vendas aparecerão aqui.'}</p><small>Solicitado em {date(profile.created_at)}</small></main><style>{styles}</style></div>;
  return <div className="partner-portal-shell"><Toaster richColors /><header className="partner-portal-header"><strong>GRAPHÈNE <small>PARCEIROS</small></strong><div><span>Olá, {profile.full_name.split(' ')[0]}</span><button onClick={signOut}><LogOut size={16}/> Sair</button></div></header><main className="partner-dashboard"><section className="partner-dashboard-intro"><div><span><BadgeCheck size={15}/> PARCERIA ATIVA</span><h1>Seu painel de resultados</h1><p>Dados de vendas confirmadas e valores processados pelo programa.</p></div><button className="partner-refresh" onClick={load}><RefreshCw size={15}/> Atualizar</button></section><section className="partner-coupon"><div><TicketPercent/><div><span>SEU CUPOM</span><strong>{data.coupon?.code || profile.referral_code}</strong><small>{data.coupon?.discount_type === 'percentage' ? `${data.coupon.discount_value}% de desconto para o cliente` : 'Código ativo'}</small></div></div><button onClick={copyCoupon}><Copy size={16}/> Copiar</button></section><section className="partner-metrics"><article><ReceiptText/><span>Vendas confirmadas<strong>{metrics.paidSalesCount}</strong></span></article><article><CircleDollarSign/><span>Volume vendido<strong>{money(metrics.paidSalesAmount)}</strong></span></article><article><Clock3/><span>Em prazo de segurança<strong>{money(metrics.pendingCommission)}</strong></span></article><article><WalletCards/><span>Disponível para repasse<strong>{money(metrics.availableCommission)}</strong></span></article></section><section className="partner-panel"><div className="partner-panel-heading"><div><h2>Vendas pelo seu cupom</h2><p>Somente pedidos com pagamento confirmado entram no desempenho.</p></div><strong>{data.coupon?.redeemed_count || 0} usos</strong></div>{data.orders?.length ? <div className="partner-table-wrap"><table><thead><tr><th>Pedido</th><th>Data</th><th>Venda</th><th>Pagamento</th></tr></thead><tbody>{data.orders.slice(0,15).map(order => <tr key={order.id}><td>#{order.id.slice(-8).toUpperCase()}</td><td>{date(order.created_at)}</td><td>{money(order.total)}</td><td><span className={`partner-order-status ${order.payment_status}`}>{order.payment_status === 'paid' ? 'Confirmado' : 'Pendente'}</span></td></tr>)}</tbody></table></div> : <div className="partner-empty">As vendas vinculadas ao seu cupom aparecerão aqui.</div>}</section><section className="partner-panel"><div className="partner-panel-heading"><div><h2>Histórico de comissões</h2><p>O repasse é registrado após a liberação.</p></div><strong>Pago: {money(metrics.paidCommission)}</strong></div>{data.commissions?.length ? <div className="partner-table-wrap"><table><thead><tr><th>Pedido</th><th>Comissão</th><th>Liberada em</th><th>Status</th></tr></thead><tbody>{data.commissions.slice(0,15).map(item => <tr key={item.id}><td>#{item.order_id.slice(-8).toUpperCase()}</td><td>{money(item.amount)}</td><td>{date(item.available_at)}</td><td><span className={`partner-order-status ${item.status}`}>{item.status === 'pending' ? 'Em análise' : item.status === 'available' ? 'Liberada' : item.status === 'paid' ? 'Paga' : 'Cancelada'}</span></td></tr>)}</tbody></table></div> : <div className="partner-empty">Nenhuma comissão gerada ainda.</div>}</section></main><style>{styles}</style></div>;
};

const styles = `.partner-portal-shell,.partner-portal-loading{min-height:100vh;background:radial-gradient(circle at 90% 0,rgba(39,203,230,.12),transparent 31rem),#071018;color:#eaf2f8}.partner-portal-loading{display:flex;align-items:center;justify-content:center;color:#9fb2c3}.partner-portal-header{height:72px;padding:0 max(24px,calc((100vw - 1180px)/2));display:flex;justify-content:space-between;align-items:center;border-bottom:1px solid rgba(203,213,225,.1);background:rgba(6,15,24,.78)}.partner-portal-header strong{color:#fff;letter-spacing:.06em}.partner-portal-header small{margin-left:5px;color:#62ddf2;font-size:.58rem;letter-spacing:.11em}.partner-portal-header>div{display:flex;align-items:center;gap:17px;font-size:.8rem;color:#9eb1c2}.partner-portal-header button,.partner-refresh,.partner-coupon button{display:flex;align-items:center;gap:6px;border:1px solid rgba(203,213,225,.14);border-radius:8px;background:transparent;color:#d5e1ea;padding:8px 10px;font-size:.72rem;font-weight:800;cursor:pointer}.partner-dashboard{width:min(1180px,calc(100% - 40px));margin:0 auto;padding:48px 0 72px;display:grid;gap:16px}.partner-dashboard-intro{display:flex;justify-content:space-between;align-items:end;gap:15px}.partner-dashboard-intro span,.partner-status-card>span{display:flex;align-items:center;gap:6px;color:#61dcf1;font-size:.67rem;font-weight:900;letter-spacing:.1em}.partner-dashboard h1{margin:8px 0 4px;color:#fff;font-size:2rem}.partner-dashboard-intro p{color:#91a5b7;font-size:.8rem}.partner-coupon{display:flex;justify-content:space-between;align-items:center;padding:18px;border:1px solid rgba(100,221,242,.24);border-radius:14px;background:linear-gradient(110deg,rgba(22,63,80,.72),rgba(11,25,39,.86))}.partner-coupon>div{display:flex;align-items:center;gap:13px}.partner-coupon svg{color:#69e2f5}.partner-coupon span,.partner-coupon small{display:block;color:#9cb1c2;font-size:.69rem}.partner-coupon strong{display:block;margin:3px 0;color:#fff;font-size:1.25rem;letter-spacing:.08em}.partner-coupon button{border-color:rgba(103,221,243,.3);color:#78e6f6}.partner-metrics{display:grid;grid-template-columns:repeat(4,1fr);gap:12px}.partner-metrics article{display:flex;gap:10px;padding:16px;border:1px solid rgba(203,213,225,.11);border-radius:13px;background:rgba(11,25,39,.8)}.partner-metrics svg{width:19px;color:#68def2}.partner-metrics span{display:grid;gap:3px;color:#98aabd;font-size:.68rem}.partner-metrics strong{color:#fff;font-size:1.05rem}.partner-panel{padding:19px;border:1px solid rgba(203,213,225,.11);border-radius:14px;background:rgba(11,25,39,.8)}.partner-panel-heading{display:flex;justify-content:space-between;gap:15px;padding-bottom:14px;border-bottom:1px solid rgba(203,213,225,.08)}.partner-panel h2{font-size:1rem;color:#fff}.partner-panel p{margin-top:3px;color:#8fa4b6;font-size:.72rem}.partner-panel-heading>strong{font-size:.76rem;color:#b9c9d6}.partner-table-wrap{overflow:auto}.partner-panel table{width:100%;min-width:620px;border-collapse:collapse;font-size:.77rem}.partner-panel th{padding:12px 8px;text-align:left;color:#8fa4b6;font-size:.65rem}.partner-panel td{padding:12px 8px;border-top:1px solid rgba(203,213,225,.07);color:#d8e4ed}.partner-order-status{display:inline-flex;padding:4px 7px;border-radius:99px;font-size:.63rem;font-weight:800;color:#f8d47b;background:rgba(251,191,36,.12)}.partner-order-status.paid,.partner-order-status.available{color:#71e4b5;background:rgba(36,211,154,.12)}.partner-order-status.paid{color:#71e4b5}.partner-order-status.cancelled{color:#f8a5b6;background:rgba(251,113,133,.12)}.partner-empty{min-height:110px;display:flex;align-items:center;justify-content:center;color:#8fa4b6;font-size:.8rem}.partner-status-card{width:min(560px,calc(100% - 40px));margin:12vh auto;padding:36px;border:1px solid rgba(203,213,225,.14);border-radius:18px;background:#0c1c2c}.partner-status-card>svg{width:26px;height:26px;margin-bottom:17px;color:#66dff3}.partner-status-card h1{margin:10px 0;color:#fff;font-size:1.7rem}.partner-status-card p{color:#9db0c0;line-height:1.6}.partner-status-card small{display:block;margin-top:22px;color:#7790a5}@media(max-width:760px){.partner-dashboard{width:min(100% - 28px,1180px);padding:29px 0 52px}.partner-dashboard-intro{align-items:start;flex-direction:column}.partner-metrics{grid-template-columns:repeat(2,1fr)}.partner-portal-header>div>span{display:none}.partner-coupon{align-items:flex-start;gap:14px;flex-direction:column}.partner-panel-heading{align-items:start;flex-direction:column}}`;
export default PartnerPortal;
